export const ERROR_PREFIX = `[blain]`
export const CONFIGURE_PREFIX = `()`
export const API_INCLUDE_PREFIX = `include()`
export const API_EXCLUDE_PREFIX = `exclude()`

export const CONFIG_FIELD_NAMES = {
  GROUPS: `groups`,
  BASE_GROUP: `baseGroup`,
}
